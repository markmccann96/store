# Point this to your image; e.g., from the CI workflow
APP_IMAGE=ghcr.io/<owner>/<repo>:latest

POSTGRES_DB=appdb
POSTGRES_USER=appuser
POSTGRES_PASSWORD=changeme

# Use if you have a "local" Spring profile; otherwise remove
SPRING_PROFILES_ACTIVE=local

# Optional extra JVM flags
# JAVA_OPTS=-Xms256m -Xmx512m -XX:MaxRAMPercentage=75.0 -XX:+UseG1GC
